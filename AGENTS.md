# 全国停電情報ダッシュボード

## プロジェクト概要

全国10電力ネットワーク会社の公式サイトから停電情報をスクレイピングし、リアルタイムおよび履歴データを可視化するStreamlitダッシュボード。

## ファイル構成

```
outage-dashboard/
├── app.py                    # Streamlitアプリ本体（UI・ビジュアライゼーション全般）
├── modules/
│   ├── scraper.py            # 各電力会社からのデータ取得ロジック
│   └── data_generator.py     # 都道府県マスタ・起因カテゴリー定義
├── requirements.txt
└── .streamlit/config.toml
```

## 起動方法

```bash
pip install -r requirements.txt
streamlit run app.py
```

## アーキテクチャ

### データフロー

1. `modules/scraper.py` の各 `fetch_*` 関数が各社サイトからデータ取得
2. `fetch_all_realtime()` がリアルタイムデータを47都道府県分の DataFrame に集約
3. `fetch_all_history_with_causes()` が全社の停電履歴（起因付き）を統一 DataFrame に集約
4. `app.py` が `@st.cache_data` でキャッシュしてUIに表示

### キャッシュ設定（`app.py`）

| 関数 | TTL | 用途 |
|------|-----|------|
| `load_realtime_data()` | 60秒 | 全社リアルタイム |
| `load_tohoku_realtime()` | 60秒 | 東北電力NWリアルタイム |
| `load_tohoku_detail()` | 300秒 | 東北電力NW詳細履歴 |
| `load_history_data()` | 3600秒 | 全社停電履歴 |
| `load_news()` | 600秒 | Google Newsニュース |

## 各電力会社のデータ取得方式

| 会社 | 方式 | 対象都道府県 | 履歴取得 |
|------|------|------------|---------|
| 北海道電力NW | HTML解析 | 北海道 | ○ 過去7日・起因付き |
| 東北電力NW | JSON API | 青森/岩手/宮城/秋田/山形/福島/新潟 | ○ 過去31日・起因付き |
| 北陸電力送配電 | HTML解析 | 富山/石川/福井 | ○ 過去7日・起因付き |
| 中部電力PG | XML API | 愛知/三重/岐阜/静岡/長野 | ✕ 起因情報非公開 |
| 東京電力PG | XML API + Cookie認証 | 茨城/栃木/群馬/埼玉/千葉/東京/神奈川/山梨/静岡 | ○ 過去60日・起因付き |
| 関西電力送配電 | JSON API | 滋賀/京都/大阪/兵庫/奈良/和歌山 | ○ 過去7日・起因付き |
| 四国電力送配電 | HTML解析 | 香川/愛媛/徳島/高知 | △ 過去31日・件数のみ |
| 中国電力NW | HTML解析 | 鳥取/島根/岡山/広島/山口 | ○ 過去7日・起因付き |
| 九州電力送配電 | XML API + CSV | 福岡/佐賀/長崎/熊本/大分/宮崎/鹿児島 | ○ 過去7日・起因付き |
| 沖縄電力 | XML API | 沖縄 | △ JS動的レンダリングのため制限あり |

## 起因フラグ分類（`WEATHER_FLAG_CONFIG`）

各停電起因を以下の5フラグに分類（複数フラグの `|` 区切り対応）:

- `天候` — 気象現象（氷雪・風雨・雷）
- `樹木・倒木` — 樹木の接触・倒木
- `設備` — 電力会社・顧客側設備の故障
- `外的要因` — 車両衝突・火災・鳥獣など
- `不明` — 調査中・原因特定不能

## 停電レベル定義

| ラベル | 範囲 | 色 |
|--------|------|----|
| 停電なし | 0軒 | 緑 `#4ade80` |
| 〜100軒 | 1〜100軒 | 薄黄 `#fef08a` |
| 〜1,000軒 | 101〜1,000軒 | 黄 `#fbbf24` |
| 〜10,000軒 | 1,001〜10,000軒 | 橙 `#f97316` |
| 10,000軒以上 | 10,001軒〜 | 赤 `#dc2626` |
| データ未取得 | — | グレー `#cbd5e1` |

## ナビゲーション構造（セッション状態 `active_section`）

| セクション | 内容 |
|-----------|------|
| `realtime` | 全社リアルタイム停電マップ・リスト |
| `cause` | 全社横断・事故起因集計 |
| `tohoku_rt` | 東北電力NW リアルタイム（別枠） |
| `tohoku_hist` | 東北電力NW 履歴分析（別枠） |
| `company_{key}` | 各社詳細ビュー（`_COMPANY_DETAIL_CONFIG` の key） |
| `sns` | X（旧Twitter）検索埋め込み |
| `news` | Google News RSS 停電ニュース |

## 東京電力の認証について

東京電力PGはCookieベースの認証が必要。`scraper.py` の定数を参照:

```python
_TEPCO_COOKIE_NAME  = "teideninfo-auth"
_TEPCO_COOKIE_VALUE = "sk3PT518"
```

Cookieが無効になった場合はここを更新する。

## 注意事項

- 各社サイトの仕様変更でスクレイパーが壊れることがある。エラー時は `None` を返して `取得不可` 扱いにしている。
- 中部電力PGはトップページへの先行アクセスでセッションを確立してからXMLを取得する（CPの認証対策）。
- 九州電力はCSVを都道府県×日付で個別取得するため `time.sleep(0.3)` でレート制限をかけている。
- `data_generator.py` の `get_realtime_outages()` と `get_historical_data()` はシミュレーション用のダミーデータ生成関数（現在は未使用）。
